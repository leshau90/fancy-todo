const {wrapAsync,givesError} = require('../helpers')
const {Tag,Todo,Project,User} = require('../models')

const functions = {
    addTodo:wrapAsync((req,res)=>{
            
    }),
    getAllTodo:wrapAsync((req,res)=>{}),
    getATodo:wrapAsync((req,res)=>{}),
    addFriend:wrapAsync((req,res)=>{}),

}




